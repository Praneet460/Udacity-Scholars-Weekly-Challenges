<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0a0a0a">

    <RelativeLayout
        android:id="@+id/app_logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true">

        <ImageView
            android:id="@+id/logo_image_one"
            android:layout_width="60dp"
            android:layout_height="90dp"
            android:layout_toStartOf="@id/logo_image_three"
            android:backgroundTint="#000"
            android:src="@drawable/background_gradient_gl" />

        <ImageView
            android:id="@+id/logo_image_two"
            android:layout_width="90dp"
            android:layout_height="60dp"
            android:layout_below="@id/logo_image_one"
            android:backgroundTint="#000"
            android:src="@drawable/background_gradient_rl" />

        <ImageView
            android:id="@+id/logo_image_three"
            android:layout_width="90dp"
            android:layout_height="60dp"
            android:layout_above="@id/logo_image_four"
            android:layout_toEndOf="@id/logo_image_two"
            android:backgroundTint="#000"
            android:src="@drawable/background_gradient_ll" />

        <ImageView
            android:id="@+id/logo_image_four"
            android:layout_width="60dp"
            android:layout_height="90dp"
            android:layout_below="@id/logo_image_one"
            android:layout_toEndOf="@id/logo_image_two"
            android:backgroundTint="#000"
            android:src="@drawable/background_gradient" />
    </RelativeLayout>
    <TextView
        android:layout_marginTop="4dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="Android Layouts"
        android:textColor="@android:color/white"
        android:textSize="24sp"
        android:fontFamily="sans-serif-condensed"
        android:layout_below="@id/app_logo"/>


</RelativeLayout>
